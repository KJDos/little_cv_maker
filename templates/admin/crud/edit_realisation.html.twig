{% extends "base.html.twig" %}

{% block body %}


    <div class="row bg-white responsive-padding border rounded mb-3">
        <div class="mb-4">
            <h3 class="text-uppercase title-margin"><span class="bg-dark text-white p-2">Modifier {{realisation.title}}</span></h3>
        </div>
        {{form_start(editRealisationForm)}}
            {{form_widget(editRealisationForm)}}
            {% include "commons/formButtons.html.twig" %}
        {{form_end(editRealisationForm)}}
    </div>
{% endblock %}