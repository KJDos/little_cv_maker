{% extends "base.html.twig" %}

{% block body %}
    <div class="row bg-white responsive-padding border rounded mb-3">
        <div class="mb-4">
            <h3 class="text-uppercase title-margin"><span class="bg-dark text-white p-2">Modifier {{formation.name}}</span></h3>
        </div>
        {{form_start(editFormationForm)}}
            {{form_widget(editFormationForm)}}
            {% include "commons/formButtons.html.twig" %}
        {{form_end(editFormationForm)}}
    </div>
{% endblock %}